@* To remove squigly lines from IPagedList you can assign using namespaces in View->Web.config *@
@model IPagedList<RestaurantListViewModel>

@{
    ViewBag.Title = "Home Page";
}

@*@using (Ajax.BeginForm(
        new AjaxOptions
        {
            HttpMethod = "get",
            InsertionMode = InsertionMode.Replace,
            UpdateTargetId = "restaurantList"
        }))
    {
        <input type="search" name="searchTerm" value="" />
        <input type="submit" value="Search by name" />
    }*@

<form method="get" action="@Url.Action("Index")" data-otf-ajax="true" data-otf-target="#restaurantList">

    <input type="search" name="searchTerm" data-otf-autocomplete="@Url.Action("AutoComplete")" value="" />
    <input type="submit" value="Search by name" />

</form>

@Html.Partial("_Restaurants", Model)